---
apply: always
---

# AGENTS.md — правила работы по проекту Airo

Этот файл описывает, как агентам работать с репозиторием. Используйте его как набор обязательных правил и быстрой навигации по контексту проекта.

## Контекст проекта (кратко)
Это шаблонный репозитории, для создания новых сервисов

Функционалы:
- пока нет функционалов

Ключевые зависимости:
- gRPC + grpc-gateway: API.
- Prometheus/Jaeger: опциональные метрики и трассировка.

Точка входа и композиция:
- `cmd/main.go` → `internal/app.App` (Init → Start → Listen → Stop).
- Инициализация: логгер, трассировка, pgx pool, миграции, сервисы/репозитории/хендлеры, gRPC и HTTP-gateway.

См. также общие правила:
- `structure.md` — карта папок и ключевых файлов.
- `architecture.md` — слои, правила зависимостей, поток данных.
- `runtime.md` — конфиги, окружение, как сервис работает в рантайме.

## Добавочные правила по данному проекту(репозиторию)

### ...
